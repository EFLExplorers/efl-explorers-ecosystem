# Prod chaos inbox (optional)

Drop **production-oriented** scratch here: Vercel project notes, env name checklists, rollout steps, incident timelines.

Still **not** Nextra—keep the **structured** customer-facing story in `apps/docs/content/`.

Do not commit secret values; names and pointers only.
