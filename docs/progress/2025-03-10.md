# Progress — 2025-03-10

## Summary

Theme audit for Teachers Platform UI (dark/light mode + color variants). Documented current setup, identified risks, and outlined fix order.

## Completed

- **Theme audit document** (`docs/platforms/teacher-platform/ui/theme-audit.md`)
  - How theme system works (data attributes, storage, bootstrap)
  - CSS variable cascade and light-mode contrast risk
  - Critical issues: undefined `--muted`, `--color-border`, `--color-bg`; Tailwind config mismatch; light-mode text contrast
  - Full list of hardcoded colors by file (materials, messages, bookmarks, students, settings, dashboard cards, etc.)
  - Components that correctly use theme variables
  - Suggested fix order (1–5)
  - Quick reference table for theme variables
- **Updated** `docs/platforms/teacher-platform/ui/styles.md` with link to theme audit

## Next Steps (recommended)

1. Add `--muted` and fix Chart `--color-*` in `variables.css`
2. Adjust color blocks for light-mode text contrast (compound selectors or remove text overrides)
3. Replace hardcoded colors in high-traffic pages (materials, messages, students, dashboard cards)
4. Align Tailwind config with actual design tokens (or define missing vars)

## Notes

- App uses CSS Modules + `var()`; Tailwind color utilities are configured but not heavily used; variables use hex, not HSL.
- Color swatches in settings (`.colorSwatchPurple` etc.) intentionally use hardcoded hex for preview — that’s fine.
