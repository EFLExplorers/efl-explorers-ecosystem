# Ecosystem docs manifest (this site)

- **Role:** Human-readable source of truth for architecture, platform boundaries, and contributor rules. **Canonical content lives here** under `apps/docs/content/ecosystem-docs/`.
- **Tech stack:** Next.js App Router, Nextra 4 (`nextra`, `nextra-theme-docs`), `@repo/typescript-config`, `@repo/ui` for MDX components where used.
- **Database:** No runtime Prisma usage; documentation references `packages/database/prisma/schema.prisma` as the schema source of truth.
