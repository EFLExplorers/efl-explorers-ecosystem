import { Button } from "@repo/ui/button"
import { Code } from "@repo/ui/code"

# UI Components

You can import shared components from `@repo/ui` directly in MDX pages.

## Button Example

<Button appName="docs" className="rounded-md border border-slate-300 px-4 py-2">
  Test Shared UI Button
</Button>

## Inline Code Example

<p>
  Use <Code className="rounded bg-slate-100 px-2 py-1">pnpm --filter @repo/docs dev</Code> to run docs only.
</p>

## Import Patterns

```tsx
import { Button } from "@repo/ui/button"
import { Code } from "@repo/ui/code"
```

## Documenting Alien/Chibi Components

For each new component page, include:

1. component purpose
2. supported props
3. interactive example
4. implementation notes
